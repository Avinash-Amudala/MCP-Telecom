"""Transport layer implementations for MCP-Telecom.

Supports multiple transport protocols:
- SSH (via Netmiko) — default, works with all vendors
- NETCONF (via ncclient) — structured XML/YANG data retrieval
"""
